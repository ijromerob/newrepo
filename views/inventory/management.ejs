<% if (title) { %>
<h1><%= title %></h1>
<% } else { res.redirect('/') } %> <% messages() %> <% if (errors) { %>
<ul class="notice">
  <% errors.array().forEach(error => { %>
  <li><%= error.msg %></li>
  <% }) %>
</ul>
<% } %>

<div class="inv-menu">
  <a href="/inv/add-classification">Add new classification</a>
  <a href="/inv/add-invetory">Add new inventory</a>
</div>
